#include "uwb.h"


float distance(float *src, int time, int distance, float fps, float *energy,
               int min_distance_index, int max_distance_index, int interference_range_index, float sensitivity, int blob_size);
